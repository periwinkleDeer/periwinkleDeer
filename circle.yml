machine:
  node:
    version: 0.12.0

general:
  build_dir: test

dependencies: 
  pre:
    - package.json

# ## Database
# database:
#   override:
#     - psql -u ubuntu circle_test < 

## Tests
test:
  override: 
    - mocha test


